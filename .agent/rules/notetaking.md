---
trigger: always_on
description: Rules for managing notes and attachments in the vault.
---

- When adding an image to a Markdown (.md) file:
  - Create an `attachments` subfolder in the same directory as the .md file.
  - Store the image file within that `attachments` folder.
  - Reference the image in the .md file using the relative path.


